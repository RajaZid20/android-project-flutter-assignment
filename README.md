1) Controllers are used to give control to the parent widget over its child state.
   snappingSheetController is the class used to implement the controller pattern in the snapping_sheet.
   It allows controling multiple features such as :
   snapPositions, snapToPosition method which can change the snap position.

2) "snappingCurve"

3) Inkwell's advantage over GestureDetector is that it produces a Material Design ripple effect for the various tabs.
   GestureDetector's advantage over inkwell is that it allows more control and custom effects that don't necessarily
   follow the material design guidelines.